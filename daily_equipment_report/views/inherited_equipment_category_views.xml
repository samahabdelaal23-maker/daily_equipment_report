<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_equipment_category_form" model="ir.ui.view">
        <field name="name">maintenance.equipment.category.form.inherit.daily.report</field>
        <field name="model">maintenance.equipment.category</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_category_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_questions"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-check-square-o">
                    <field name="question_count" widget="statinfo" string="Questions"/>
                </button>
            </xpath>

            <xpath expr="//sheet" position="inside">
                <notebook>
                    <page string="Inspection Checklist" name="inspection_checklist">
                        <div class="alert alert-info" role="alert">
                            Add the questions that must be checked whenever equipment in this category is inspected.
                            Use the handle to control their order.
                        </div>
                        <field name="question_ids" context="{'default_category_id': id}">
                            <list editable="bottom" default_order="sequence, id">
                                <field name="sequence" widget="handle"/>
                                <field name="name" placeholder="e.g. Is the oil level acceptable?"/>
                                <field name="failure_severity"
                                       string="Failure Severity"
                                       required="1"/>
                                <field name="active" optional="hide"/>
                            </list>
                        </field>
                    </page>
                    <page string="Equipment" name="daily_report_equipment">
                        <field name="equipment_ids" readonly="1">
                            <list create="false" delete="false">
                                <field name="name"/>
                                <field name="model"/>
                                <field name="serial_no"/>
                                <field name="owner_user_id"/>
                                <field name="operational_status" widget="badge"
                                       decoration-success="operational_status == 'active'"
                                       decoration-warning="operational_status == 'maintenance'"
                                       decoration-danger="operational_status == 'out_of_service'"/>
                                <field name="last_report_date"/>
                            </list>
                        </field>
                    </page>
                </notebook>
            </xpath>
        </field>
    </record>

    <record id="view_equipment_category_list" model="ir.ui.view">
        <field name="name">maintenance.equipment.category.list.inherit.daily.report</field>
        <field name="model">maintenance.equipment.category</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_category_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="equipment_count"/>
                <field name="question_count"/>
            </xpath>
        </field>
    </record>

    <record id="action_equipment_category" model="ir.actions.act_window">
        <field name="name">Equipment Categories</field>
        <field name="res_model">maintenance.equipment.category</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="view_id" ref="maintenance.hr_equipment_category_view_tree"/>
    </record>
</odoo>
